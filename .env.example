# Copy to .env and edit as needed. All vars are optional — defaults work for local dev.

SI_DB_PATH=store_intelligence.db
SI_POS_CSV=data/pos_transactions.csv
SI_LAYOUT_PATH=config/store_layout.json
SI_STORES_PATH=config/stores.yaml
SI_STALE_FEED_MINUTES=10
SI_SESSION_TIMEOUT_S=1800
SI_REENTRY_GAP_S=60
SI_POS_WINDOW_MINUTES=5

# Optional: OpenAI/Anthropic key for VLM staff spot-check (pipeline only, not required for API)
# VLM_API_KEY=
